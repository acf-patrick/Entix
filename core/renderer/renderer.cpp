/* #include "renderer.h"
#include <cassert>

Renderer* Renderer::instance = nullptr;
Renderer& Renderer::get()
{ return *instance; }
void Renderer::clean()
{
    delete instance;
    instance = nullptr;
}

Renderer::Renderer(const std::string& title, const SDL_Point& windowSize, const SDL_Point& windowPos)
{
    
} */